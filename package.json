{
  "name": "project00-root",
  "private": true,
  "packageManager": "npm@11.1.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "format": "npx @biomejs/biome format --write code",
    "lint": "npx @biomejs/biome ci code"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.0.0",
    "turbo": "^2.4.4",
    "typescript": "5.8.2",
    "wrangler": "^3.107.3"
  },
  "workspaces": ["code/apps/*", "code/packages/*"],
  "engines": {
    "node": ">=22.0.0"
  }
}
